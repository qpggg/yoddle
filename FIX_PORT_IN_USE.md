# 🔧 РЕШЕНИЕ: Port 3001 уже занят (EADDRINUSE)

## ❌ Проблема:

```
Error: listen EADDRINUSE: address already in use 0.0.0.0:3001
```

Порт 3001 уже используется другим процессом.

## ✅ РЕШЕНИЕ:

### ШАГ 1: Проверьте процессы PM2

```bash
pm2 list
```

Если видите несколько процессов `yoddle-api` или старые процессы, удалите их:

```bash
pm2 stop all
pm2 delete all
```

### ШАГ 2: Проверьте, какой процесс использует порт 3001

```bash
# Вариант 1: Используя lsof
lsof -i :3001

# Вариант 2: Используя netstat
netstat -tulpn | grep 3001

# Вариант 3: Используя ss
ss -tulpn | grep 3001
```

### ШАГ 3: Убейте процесс, который занимает порт

Если нашли процесс (PID будет показан в выводе):

```bash
# Замените <PID> на реальный номер процесса
kill -9 <PID>
```

Или убейте все процессы Node.js (осторожно!):

```bash
pkill -9 node
```

### ШАГ 4: Проверьте, что порт свободен

```bash
lsof -i :3001
# Должно быть пусто
```

### ШАГ 5: Запустите приложение заново

```bash
cd /root/yoddle
pm2 start ecosystem.config.cjs
pm2 status
pm2 logs yoddle-api --lines 30
```

---

## 🎯 БЫСТРОЕ РЕШЕНИЕ (если уверены):

```bash
# Остановить все PM2 процессы
pm2 stop all
pm2 delete all

# Убить все процессы Node.js на порту 3001
fuser -k 3001/tcp

# Или через pkill
pkill -9 node

# Подождать секунду
sleep 2

# Запустить заново
cd /root/yoddle
pm2 start ecosystem.config.cjs
pm2 status
```

---

## 📋 ЧЕКЛИСТ:

- [ ] Проверены процессы PM2 (`pm2 list`)
- [ ] Все старые процессы PM2 удалены
- [ ] Проверен порт 3001 (`lsof -i :3001`)
- [ ] Процесс, занимающий порт, убит
- [ ] Порт свободен
- [ ] Приложение запущено через PM2
- [ ] Логи показывают успешный запуск

Выполните команды и сообщите результат!

