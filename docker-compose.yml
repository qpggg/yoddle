services:
  app:
    image: node:20
    working_dir: /app
    ports:
      - "3001:3001"
    environment:
      - NODE_ENV=development
      - PORT=3001
      - PG_CONNECTION_STRING=postgresql://f1111323_yoddle:Nei3wmOK@localhost:6543/supa_full?sslmode=disable
    volumes:
      - .:/app
      - /app/node_modules
    command: sh -c "npm ci --no-audit --no-fund || npm install --no-audit --no-fund && npm run dev:server"
    restart: unless-stopped