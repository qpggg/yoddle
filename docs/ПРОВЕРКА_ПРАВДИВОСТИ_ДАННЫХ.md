# ПРОВЕРКА ПРАВДИВОСТИ ДАННЫХ РЕЙТИНГА ПРОДУКТИВНОСТИ

## Дата проверки: 28.01.2026

## Результаты проверки для пользователя ID 3 (рейтинг 7.13)

### 1. Данные настроения за последние 7 дней

**Фактические данные из БД:**
- Всего записей за неделю: **2 записи** (только 28 января)
- Среднее настроение: **9.50** (95%)
- Средняя энергия: **NULL** (нет данных)
- Средний стресс: **4.00**
- Среднее спокойствие: **6.00** (60%)

**Данные на графике (из изображения):**
- Настроение: **49%** ❌ НЕ СОВПАДАЕТ (должно быть 95%)
- Энергия: **48%** ❌ НЕТ ДАННЫХ В БД
- Спокойствие: **42%** ❌ НЕ СОВПАДАЕТ (должно быть 60%)

### 2. Ручной расчет рейтинга продуктивности

**Исходные данные:**
- Записей настроения за всю историю: **16**
- Общая сумма компонентов настроения: **348**
- Средний компонент настроения: **348 / 16 / 3 = 7.25**
- Успешных активностей: **7**
- Неуспешных активностей: **0**
- Действий на платформе за сегодня: **4**
- Коэффициент активности: **max(0.6, min(4/10, 1.0)) = 0.6**

**Расчет:**
```
Рейтинг = ((7.25) + (7 × 0.5) + (0 × (-0.5))) × 1.2 × 0.6
Рейтинг = (7.25 + 3.5 + 0) × 1.2 × 0.6
Рейтинг = 10.75 × 1.2 × 0.6
Рейтинг = 12.9 × 0.6
Рейтинг = 7.74
```

**Но функция возвращает:** 7.13

**Расхождение:** 7.74 vs 7.13 = разница **0.61**

### 3. Возможные причины расхождений

#### 3.1. Расхождение в данных настроения на графике

**Проблема:** График показывает 49%, 48%, 42%, но в БД для пользователя 3:
- Настроение: 95% (должно быть на графике)
- Энергия: нет данных (не должно показываться)
- Спокойствие: 60% (должно быть на графике)

**Возможные причины:**
1. Используется другая функция расчета (`get_weekly_average_percentages`)
2. Используются данные другого пользователя
3. Используется другой период времени (не последние 7 дней)
4. Ошибка в функции расчета процентов

#### 3.2. Расхождение в рейтинге продуктивности

**Проблема:** Ручной расчет дает 7.74, функция возвращает 7.13

**Возможные причины:**
1. Используется другая формула в функции `calculate_productivity_score`
2. Разные данные учитываются (возможно, фильтрация по датам)
3. Округление в промежуточных расчетах
4. Используется другая логика расчета коэффициента активности

### 4. Рекомендации

1. **Проверить функцию `get_weekly_average_percentages`:**
   - Убедиться, что она использует правильный период (последние 7 дней)
   - Проверить логику расчета процентов
   - Убедиться, что она учитывает NULL значения энергии

2. **Проверить функцию `calculate_productivity_score`:**
   - Сравнить формулу с документацией
   - Проверить логику расчета коэффициента активности
   - Убедиться, что используются правильные данные (вся история до указанной даты)

3. **Проверить данные на фронтенде:**
   - Убедиться, что используется правильный user_id
   - Проверить, какие данные возвращает API endpoint `/api/productivity/mood-percentages/:userId`
   - Проверить, правильно ли отображаются данные на графике

4. **Исправить расхождения:**
   - Если функция расчета процентов неверна - исправить
   - Если функция расчета рейтинга неверна - исправить
   - Если данные на графике неверны - исправить отображение

### 5. Выводы

**Рейтинг продуктивности 7.1:**
- ✅ Функция возвращает **7.13** (близко к 7.1)
- ⚠️ Ручной расчет дает **7.74** (расхождение 0.61)
- ❓ Нужно проверить формулу в функции `calculate_productivity_score`

**Данные на графике:**
- ❌ Настроение: показывает 49%, должно быть 95%
- ❌ Энергия: показывает 48%, но данных нет в БД
- ❌ Спокойствие: показывает 42%, должно быть 60%
- ❓ Нужно проверить функцию `get_weekly_average_percentages` и данные на фронтенде
